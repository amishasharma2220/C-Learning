#include<stdio.h>
int main(){
    int age=18;
    printf("%d\n",age>=18); //This will check if age is greater than or equal to 18 and return 1 if true and 0 if false which will be 1.
    printf("%d\n",age<18); //This will check if age is less than 18 and return 1 if true and 0 if false which will be 0.
    return 0;
}